{
  "name": "@js-template-engine/core",
  "version": "1.0.1",
  "description": "Core template engine functionality",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rimraf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@js-template-engine/types": "workspace:*",
    "@types/prettier": "^3.0.0",
    "@types/signale": "^1.4.4",
    "prettier": "^3.5.3",
    "signale": "^1.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^1.3.1",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3",
    "vitest": "^1.6.1"
  }
}