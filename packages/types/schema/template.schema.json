{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/Template",
  "definitions": {
    "Template": {
      "anyOf": [
        {
          "$ref": "#/definitions/ComponentNode"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/definitions/TemplateNode"
          }
        }
      ],
      "description": "A complete template: either a root `ComponentNode` or a bare node array. Templates without a component wrapper are valid; the engine uses a default component name from options."
    },
    "ComponentNode": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "component"
        },
        "name": {
          "type": "string",
          "description": "The component name: `'Button'` → `Button.tsx` / `Button.vue` / ..."
        },
        "props": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/PropDefinition"
          },
          "description": "Component props."
        },
        "imports": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Verbatim import statements."
        },
        "script": {
          "type": "string",
          "description": "Framework-agnostic JS (handlers, utils)."
        },
        "style": {
          "type": "string",
          "description": "Component-level CSS, distributed to children."
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/TemplateNode"
          }
        },
        "extensions": {
          "$ref": "#/definitions/ComponentExtensionOverrides",
          "description": "Component-level per-extension overrides."
        }
      },
      "required": [
        "type",
        "name",
        "children"
      ],
      "additionalProperties": false,
      "description": "The optional root wrapper carrying component metadata. Validation rejects `component` nodes anywhere but the template root."
    },
    "PropDefinition": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "description": "A TypeScript type expression: `\"'primary' | 'secondary'\"`, `'boolean'`."
        },
        "required": {
          "type": "boolean",
          "description": "Whether the prop is required. Defaults to `false`."
        },
        "default": {
          "$ref": "#/definitions/JsonValue",
          "description": "A JSON-serializable default value."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "A component prop definition.\n\nRendered as a generated props interface with destructured defaults in React, `defineProps` in Vue, `export let` declarations in Svelte, and plain consts in the HTML-mode script block."
    },
    "JsonValue": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "number"
        },
        {
          "type": "boolean"
        },
        {
          "type": "null"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/definitions/JsonValue"
          }
        },
        {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/JsonValue"
          }
        }
      ],
      "description": "A JSON-serializable value.\n\nTemplates are plain serializable data: anything expressible in the template types is expressible in JSON, so values that must survive serialization are constrained to this type."
    },
    "TemplateNode": {
      "anyOf": [
        {
          "$ref": "#/definitions/ComponentNode"
        },
        {
          "$ref": "#/definitions/ElementNode"
        },
        {
          "$ref": "#/definitions/TextNode"
        },
        {
          "$ref": "#/definitions/CommentNode"
        },
        {
          "$ref": "#/definitions/FragmentNode"
        },
        {
          "$ref": "#/definitions/SlotNode"
        },
        {
          "$ref": "#/definitions/ConditionalNode"
        },
        {
          "$ref": "#/definitions/IterationNode"
        }
      ],
      "description": "Any template node.\n\nEvery node has a `type` discriminant; unknown `type` values are a validation error. `ComponentNode` is structurally part of the union but only valid at the template root."
    },
    "ElementNode": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "element"
        },
        "tag": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/definitions/DynamicTag"
            }
          ],
          "description": "The tag name: `'div'`, `'button'`, `'svg'`, ... A `DynamicTag` resolves the tag from a runtime expression instead (root element only)."
        },
        "attributes": {
          "$ref": "#/definitions/Attributes",
          "description": "Static and dynamic attributes."
        },
        "conditionalAttributes": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ConditionalAttributes"
          },
          "description": "Condition-gated attributes."
        },
        "events": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/EventDefinition"
          },
          "description": "Events bound to this element."
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/TemplateNode"
          }
        },
        "extensions": {
          "$ref": "#/definitions/ExtensionOverrides",
          "description": "Per-extension overrides."
        },
        "slotScope": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Names the runtime values a composed component exposes through its default scoped slot, bringing them into scope of this node's projected `children`. Valid only on a component-reference node (a capitalized tag); each entry is a binding identifier the composed component's default slot exposes (its `SlotNode.exposes` keys). The children may then reference those identifiers freely.\n\nPer target: React wraps the children as a function child (`{({ api }) => (...)}`), Vue emits `v-slot=\"{ api }\"`, Svelte emits `let:api`. HTML mode ignores it (a static preview has no consumer). Only the default slot is supported."
        },
        "slots": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/NamedSlotContent"
          },
          "description": "Content projected into a composed child's **named** slots, keyed by slot name (the child's  {@link  SlotNode.name } ); this node's `children` continue to fill the default slot. Valid only on a component-reference node (a capitalized tag). Each value is the projected content (an ordinary named slot), or a `{ content, slotScope? }` object whose `slotScope` names the bindings received from that named slot's exposed scope (a scoped named slot), brought into scope of `content`.\n\nPer target: React passes each as the slot's prop (`closeButton={...}`, a render prop `closeButton={({ row }) => ...}` when scoped); Vue emits `<template #closeButton>` (`#closeButton=\"{ row }\"` when scoped); Svelte emits `<svelte:fragment slot=\"closeButton\">` (`let:row` when scoped). HTML mode ignores it (a static preview has no slot consumer). The provider seam is  {@link  SlotNode }  (a named, optionally scoped, slot)."
        },
        "passthrough": {
          "type": "boolean",
          "description": "Marks this element as the component's surface root: a consumer's undeclared props/attributes spread onto it, a consumer-supplied `className`/`class` and `style` merge into its class list and styles, and it exposes a DOM handle (React ref-as-prop, Vue `$el`, Svelte a bindable `element`). At most one element per component may set it, and it must be the component's single root rendered element with an intrinsic HTML tag."
        }
      },
      "required": [
        "type",
        "tag"
      ],
      "additionalProperties": false
    },
    "DynamicTag": {
      "type": "object",
      "properties": {
        "$expression": {
          "type": "string",
          "description": "The runtime tag expression, e.g. `'as'` - a JavaScript expression."
        },
        "default": {
          "type": "string",
          "description": "The static fallback tag - preview, validation, and typing basis."
        }
      },
      "required": [
        "$expression",
        "default"
      ],
      "additionalProperties": false,
      "description": "A dynamic tag: the element's tag name is resolved from a runtime expression instead of a literal. `$expression` is an opaque JavaScript expression (the engine emits it verbatim, never parses it) yielding the runtime tag - an intrinsic tag string or a component identifier. `default` is a required static tag: the basis for the HTML-mode static preview, the void/children validation check, and - on a passthrough surface root - the typed prop surface. The engine never injects `default` as a runtime fallback; an author wanting one writes it into the expression. Allowed only on a component's single root rendered element."
    },
    "Attributes": {
      "type": "object",
      "properties": {
        "class": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ClassEntry"
              }
            },
            {
              "$ref": "#/definitions/ExpressionBinding"
            }
          ],
          "description": "Classes. The array form is canonical; the string form is accepted, normalized, and literal-only. Array entries - or the sole value - may be `$expression` bindings: expression classes render after every other class source, in authored order among themselves, and a falsy expression value contributes nothing at runtime."
        },
        "style": {
          "$ref": "#/definitions/NestedStyleObject",
          "description": "Nested style object. A top-level `$expression` key carries a whole-object expression evaluating to a plain object of camelCase CSS property→value pairs; it may stand alone or coexist with static properties, includes, nested selectors, and per-property `$expression` values on the same node."
        },
        "$spread": {
          "anyOf": [
            {
              "$ref": "#/definitions/ExpressionBinding"
            },
            {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ExpressionBinding"
              }
            }
          ],
          "description": "Object spread. Each  {@link  ExpressionBinding }  names a runtime object whose own enumerable properties are spread onto this element (or, on a component-reference node, its props): React `{...x}`, Vue `v-bind=\"x\"`, Svelte `{...x}`. An array spreads several objects in order. Spreads lead the node's attributes - authored attributes, classes, and styles follow and override per key (consumers add, they do not replace). Unlike the passthrough root's undeclared-rest spread, `$spread` targets a *named* object and is allowed on any element, not the root alone. HTML mode is inert (no runtime consumer)."
        }
      },
      "additionalProperties": {
        "anyOf": [
          {
            "$ref": "#/definitions/AttributeValue"
          },
          {
            "type": "array",
            "items": {
              "$ref": "#/definitions/ClassEntry"
            }
          },
          {
            "$ref": "#/definitions/NestedStyleObject"
          },
          {
            "not": {}
          }
        ]
      },
      "description": "The single attribute map for both static and dynamic values.\n\n`class` and `style` have dedicated shapes. Both accept `$expression` bindings in constrained forms - expression entries in the class list (or as its sole value), expression values on top-level style properties, or a top-level `$expression` key carrying the whole-object expression (which may coexist with static and nested keys). The reserved `$spread` key spreads a named runtime object's own properties onto the element (or composed component) ahead of the authored attributes. *Condition-gated* classes and styles go through `conditionalAttributes` instead, which carries the condition explicitly and is literal-only."
    },
    "ClassEntry": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "$ref": "#/definitions/ExpressionBinding"
        }
      ],
      "description": "One entry of the `class` array form: a literal class name or an `$expression` binding contributing runtime classes."
    },
    "ExpressionBinding": {
      "type": "object",
      "properties": {
        "$expression": {
          "type": "string"
        }
      },
      "required": [
        "$expression"
      ],
      "additionalProperties": false,
      "description": "A dynamic attribute binding.\n\nThe `$` sigil marks the key as engine-reserved: `$expression` can never be a legitimate CSS property, class name, or HTML attribute, so the wrapper is unambiguous against every other value shape in the format. The expression is a JavaScript expression, treated as an opaque string and emitted verbatim into the target syntax - the engine never parses or evaluates it."
    },
    "NestedStyleObject": {
      "type": "object",
      "properties": {
        "$expression": {
          "type": "string",
          "description": "A whole-object style expression: a string evaluating to a plain object of camelCase CSS property→value pairs, rendered through the target's dynamic style mechanism. May coexist with static properties, includes, nested selectors, and per-property `$expression` values on the same node (see the interface summary for the merge order). Banned inside nested selector blocks - stylesheets cannot hold runtime values."
        },
        "$include": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          ],
          "deprecated": "Use an `@include ...` at-rule key instead\n(`'@include name': true` for a no-content include). Sass `@include`\nstatement(s), emitted verbatim under `styling.language: 'scss'`. A\ncompile-time-opaque string, or an array for several includes in authored\norder; never produces a CSS declaration. Under `styling.language: 'css'`\nthe engine resolves it against `styling.loadPaths`."
        }
      },
      "additionalProperties": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "number"
          },
          {
            "type": "boolean"
          },
          {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          {
            "$ref": "#/definitions/ExpressionBinding"
          },
          {
            "$ref": "#/definitions/NestedSelectorBlock"
          },
          {
            "not": {}
          }
        ]
      },
      "description": "A nested style object.\n\nAccepts plain CSS properties (camelCase or kebab-case), pseudo-selectors (`':hover'`), media queries (`'@media (max-width: 768px)'`), and parent-modifier selectors (`'.button--large &'`) as keys. Nested selectors always require a stylesheet and therefore force a class-based selector for the node.\n\nTop-level plain properties - CSS custom properties included - may carry `$expression` values; these render through the target framework's dynamic style mechanism and never enter generated CSS. To make the entire style dynamic, give the object a top-level `$expression` key (a string expression evaluating to a plain object of property→value pairs). It may\n**coexist** with static properties, `$include`/`@include`, nested selectors, and per-property `$expression` values on the same node: static and nested keys route to the stylesheet (or inline per strategy) as usual, while the whole-object expression renders through the dynamic mechanism. Where both reach the same inline mechanism, the merge order is whole-object first (the base layer), then authored static / per-property values, then any passthrough consumer `style` (later wins).\n\nAn `@include ...` at-rule key carries a Sass mixin include - the inverse of `$expression`: compile-time source (not runtime), legal in any block including nested selector blocks. Its value encodes whether a content block is passed:\n\n- `'@include name': true` (or `{}`) → `@include name;` (no content)\n- `'@include name': { ... }` → `@include name { ... }` (the object is the   content block)\n\nIncludes are emitted in authored order, interleaved with sibling declarations, so an include followed by an overriding declaration cascades as written. Under `styling.language: 'scss'` they pass through for the consumer's own sass build; under `'css'` (or the `inline` strategy) the engine resolves them against `styling.loadPaths`."
    },
    "NestedSelectorBlock": {
      "type": "object",
      "properties": {
        "$expression": {
          "not": {},
          "description": "`$expression` is engine-reserved and invalid inside selector blocks."
        },
        "$include": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          ],
          "deprecated": "Use an `@include ...` at-rule key instead\n(`'@include name': true` for a no-content include). Sass `@include`\nstatement(s), emitted verbatim under `styling.language: 'scss'`. A\ncompile-time-opaque string, or an array for several includes in authored\norder; never produces a CSS declaration."
        }
      },
      "additionalProperties": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "number"
          },
          {
            "type": "boolean"
          },
          {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          {
            "$ref": "#/definitions/NestedSelectorBlock"
          },
          {
            "not": {}
          }
        ]
      },
      "description": "A nested selector block inside a style object: the value of a pseudo-selector, at-rule, or parent-modifier key.\n\nNesting composes recursively - at-rules, pseudo-selectors, and parent-modifier selectors may contain one another, with at-rules wrapping outermost in the emitted CSS. Selector blocks always emit to a stylesheet, so their values are static only: `$expression` bindings are invalid inside them (stylesheets cannot hold runtime values).\n\nAn `@include ...` at-rule key carries a Sass mixin include - compile-time source. Its value encodes whether a content block is passed: `true` (or an empty object) emits a no-content `@include name;` statement, while a non-empty object emits `@include name { ... }` with that object as the content block. Like a selector block, it is valid inside nested blocks."
    },
    "AttributeValue": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "number"
        },
        {
          "type": "boolean"
        },
        {
          "$ref": "#/definitions/ExpressionBinding"
        }
      ],
      "description": "A static or dynamic attribute value.\n\nBooleans render as presence/absence: `{ disabled: true }` produces `disabled` and `{ disabled: false }` omits the attribute."
    },
    "ConditionalAttributes": {
      "type": "object",
      "properties": {
        "condition": {
          "type": "string",
          "description": "A JavaScript expression."
        },
        "attributes": {
          "$ref": "#/definitions/Attributes",
          "description": "The attributes applied when the condition holds."
        }
      },
      "required": [
        "condition",
        "attributes"
      ],
      "additionalProperties": false,
      "description": "Condition-gated attributes.\n\nRenders as a generated class expression in React, `:class` object syntax in Vue, and `class:` directives in Svelte; HTML mode omits them from markup (warning emitted) but still generates CSS for the referenced classes.\n\n`class` and `style` are literal-only here - the condition is their dynamism; `$expression` values inside conditional attributes are a validation error."
    },
    "EventDefinition": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Generic DOM event name: `'click'`, `'input'`, `'submit'`, ..."
        },
        "handler": {
          "type": "string",
          "description": "An identifier (`'handleClick'`) or inline JS (`'() => save()'`)."
        },
        "modifiers": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/EventModifier"
          },
          "description": "Optional modifiers, applied in order. At most one key modifier per event definition."
        }
      },
      "required": [
        "name",
        "handler"
      ],
      "additionalProperties": false,
      "description": "An event bound to an element.\n\nThe name is the generic DOM event name; framework extensions transform it into their own syntax (`onClick={...}`, `@click=\"...\"`, `on:click={...}`)."
    },
    "EventModifier": {
      "anyOf": [
        {
          "type": "string",
          "const": "prevent"
        },
        {
          "type": "string",
          "const": "stop"
        },
        {
          "type": "string",
          "const": "once"
        },
        {
          "type": "string",
          "const": "self"
        },
        {
          "type": "string",
          "const": "capture"
        },
        {
          "type": "string",
          "const": "passive"
        },
        {
          "$ref": "#/definitions/KeyEventModifier"
        }
      ],
      "description": "An event modifier.\n\nModifiers map natively where the target framework supports them (Vue `@click.prevent`, Svelte `on:click|preventDefault`) and are wrapped in generated handler code where it doesn't (React, HTML mode). Key-guard modifiers map natively in Vue (`@keyup.enter`) and wrap the handler in a generated `event.key` guard everywhere else."
    },
    "KeyEventModifier": {
      "type": "string",
      "enum": [
        "enter",
        "escape",
        "tab",
        "space",
        "backspace",
        "delete",
        "arrow-up",
        "arrow-down",
        "arrow-left",
        "arrow-right"
      ],
      "description": "A key-guard event modifier: the handler only runs when the corresponding `KeyboardEvent.key` value matches (`'Enter'`, `'Escape'`, `'Tab'`, `' '`, `'Backspace'`, `'Delete'`, `'ArrowUp'`, ...).\n\nAt most one key modifier is allowed per event definition - chained key guards filter on a single `event.key` and could never pass together; multiple accepted keys are written as separate event entries."
    },
    "ExtensionOverrides": {
      "type": "object",
      "additionalProperties": {},
      "description": "Per-extension node overrides.\n\nAny node may carry overrides under `extensions.<key>`, where `key` is the extension's declared name (`'react'`, `'vue'`, `'bem'`, `'tailwind'`, ...). The override shape is defined by each extension and typed via module augmentation of this interface, so `defineTemplate` autocompletes installed extensions' options.\n\nResolution priority: extension-specific override → generic concept → nothing. Overrides for extensions not present in `ProcessingOptions.extensions` are inert, not errors."
    },
    "NamedSlotContent": {
      "anyOf": [
        {
          "type": "array",
          "items": {
            "$ref": "#/definitions/TemplateNode"
          }
        },
        {
          "type": "object",
          "properties": {
            "content": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/TemplateNode"
              }
            },
            "slotScope": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "content"
          ],
          "additionalProperties": false
        }
      ],
      "description": "Content a composing node projects into one of a composed child's named slots (see  {@link  ElementNode.slots } ).\n\nThe shorthand array form is the projected content (an ordinary named slot). The object form adds `slotScope` - binding names received from that named slot's exposed scope (a scoped named slot), brought into scope of `content`, the same mechanism as  {@link  ElementNode.slotScope }  for the default slot."
    },
    "TextNode": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "text"
        },
        "content": {
          "type": "string",
          "description": "Static text."
        },
        "expression": {
          "type": "string",
          "description": "Dynamic text, e.g. `'user.name'` - a JavaScript expression."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Static or dynamic text.\n\nExactly one of `content` | `expression` must be present. There is no inline interpolation syntax: `{{ }}` inside `content` is literal text, and mixed static/dynamic text is expressed as sibling text nodes."
    },
    "CommentNode": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "comment"
        },
        "content": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "content"
      ],
      "additionalProperties": false,
      "description": "A comment. Rendered as a comment in every target; never affects semantics."
    },
    "FragmentNode": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "fragment"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/TemplateNode"
          }
        }
      },
      "required": [
        "type",
        "children"
      ],
      "additionalProperties": false,
      "description": "Groups children without a wrapper element: React `<>...</>`, plain sibling output elsewhere."
    },
    "SlotNode": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "slot"
        },
        "name": {
          "type": "string",
          "description": "The slot name; omitted = default slot."
        },
        "fallback": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/TemplateNode"
          },
          "description": "Rendered when nothing is projected."
        },
        "exposes": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/ExposedBinding"
          },
          "description": "Runtime values this slot exposes to its projected content, keyed by the scope binding name consumers see. Each value is the provider-scope expression (and, in the object form, an optional React type). Absent = an ordinary, non-scoped slot."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "A content projection point.\n\nHTML mode renders `fallback` (slots have no consumer in static HTML); React uses the `{children}` / `{slots.<name>}` prop convention; Vue and Svelte render `<slot>` elements. Fallbacks must not contain slot nodes.\n\nA slot may expose runtime values to its projected content (`exposes`), making it a scoped slot: React turns the slot prop into a render prop (`children?: (scope) => ReactNode`), Vue binds them on the `<slot>` (`<slot :api=\"api\" />`), and Svelte binds them likewise (`<slot api={api} />`). A composing node receives them through  {@link  ElementNode.slotScope } ."
    },
    "ExposedBinding": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "object",
          "properties": {
            "value": {
              "type": "string"
            },
            "type": {
              "type": "string"
            }
          },
          "required": [
            "value"
          ],
          "additionalProperties": false
        }
      ],
      "description": "One runtime value a slot exposes to its projected content (a scoped slot).\n\nThe shorthand string form is the provider-scope JavaScript expression, typed `any` in the generated React render-prop signature. The object form adds an optional TypeScript `type` for that React signature; the type flows to a React consumer through inference from the composed component's `children` type. `type` has no effect on Vue or Svelte (legacy `<slot>` scope is untyped) and is ignored in HTML mode."
    },
    "ConditionalNode": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "conditional"
        },
        "conditions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ConditionalBranch"
          }
        }
      },
      "required": [
        "type",
        "conditions"
      ],
      "additionalProperties": false,
      "description": "A conditional with a unified branch list.\n\nThe statement sequence is validated: `if` first, then any number of `else-if`, optional trailing `else`."
    },
    "ConditionalBranch": {
      "type": "object",
      "properties": {
        "statement": {
          "$ref": "#/definitions/ConditionalStatement"
        },
        "condition": {
          "type": "string",
          "description": "A JavaScript expression."
        },
        "props": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/PropDefinition"
          },
          "description": "Props scoped to this branch of a discriminated surface root."
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/TemplateNode"
          }
        }
      },
      "required": [
        "statement",
        "children"
      ],
      "additionalProperties": false,
      "description": "One branch of a conditional.\n\n`condition` is required except for `'else'`.\n\n`props` declares props scoped to this branch - used only when the conditional is a component's discriminated surface root (each branch a single `passthrough` element of a different intrinsic tag). The props assemble into a discriminated union: each branch contributes its element's typed surface plus these branch props (typically the discriminant as a literal type, e.g. `visual: { type: 'true' }`). Props shared across branches stay on the component (`ComponentNode.props`); a name that is content in one branch and a native attribute in another lives as a shared prop bound explicitly per branch, never as a branch prop."
    },
    "ConditionalStatement": {
      "type": "string",
      "enum": [
        "if",
        "else-if",
        "else"
      ],
      "description": "A branch statement in a conditional."
    },
    "IterationNode": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "iteration"
        },
        "items": {
          "type": "string",
          "description": "An expression evaluating to an iterable: `'props.users'`."
        },
        "item": {
          "type": "string",
          "description": "The loop variable name: `'user'`."
        },
        "index": {
          "type": "string",
          "description": "Optional index variable name: `'i'`."
        },
        "key": {
          "type": "string",
          "description": "Optional expression for stable identity: `'user.id'`."
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/TemplateNode"
          }
        }
      },
      "required": [
        "type",
        "items",
        "item",
        "children"
      ],
      "additionalProperties": false,
      "description": "A loop.\n\nRenders as `.map()` in React, `v-for` in Vue, `{#each}` in Svelte; HTML mode renders children once as a static sample wrapped in debug comments."
    },
    "ComponentExtensionOverrides": {
      "type": "object",
      "additionalProperties": {},
      "description": "Component-level overrides keyed by extension name.\n\nEach value is at least a `ComponentExtensionOverride`; an extension may widen its own key through module augmentation to carry additional component-level settings (the Vue extension's `scoped` flag, for example), exactly as node-level overrides extend `ExtensionOverrides`."
    }
  }
}
