{
  "name": "js-template-engine",
  "version": "1.0.1",
  "private": true,
  "description": "A dynamic templating engine that translates Typescript/JavaScript or JSON data into structured templates across multiple languages.",
  "scripts": {
    "build": "pnpm --filter @js-template-engine/core build && pnpm --filter \"@js-template-engine/extension-*\" build && pnpm --filter @js-template-engine/cli build",
    "build:watch": "pnpm -r build:watch",
    "clean": "pnpm -r clean",
    "test": "pnpm -r test",
    "test:watch": "pnpm -r test:watch",
    "lint": "pnpm -r lint",
    "lint:fix": "pnpm -r lint:fix",
    "type-check": "pnpm -r type-check",
    "prepublishOnly": "pnpm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/djurnamn/js-template-engine.git"
  },
  "keywords": [
    "javascript",
    "js",
    "template",
    "templating",
    "engine",
    "json",
    "react",
    "bem",
    "typescript"
  ],
  "author": "Björn Djurnamn <bjorn@djurnamn.co>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/djurnamn/js-template-engine/issues"
  },
  "homepage": "https://github.com/djurnamn/js-template-engine",
  "engines": {
    "node": ">=14.0.0",
    "pnpm": ">=8.0.0"
  }
}
